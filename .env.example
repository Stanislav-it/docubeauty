# Copy this file to .env for local development.
# NEVER commit .env with real keys.

# Stripe
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Required in production (sessions + signed download tokens)
SECRET_KEY=change-me

# Optional: download links TTL (seconds)
# DOWNLOAD_TTL_SECONDS=604800

# Persistent storage (Render / other PaaS)
# If you mount a disk at /var/data, set:
# PERSIST_ROOT=/var/data
# or set explicit paths:
# DATA_DIR=/var/data/data
# UPLOADS_DIR=/var/data/uploads
