# Copy this file to .env for LOCAL development.
# NEVER commit .env with real keys.

# -------------------------
# Stripe (TEST mode)
# -------------------------
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Optional: change admin/session secret
SECRET_KEY=change-me

# -------------------------
# Persistent storage
# -------------------------
# In local dev you can leave these empty (defaults to ./data and ./static/uploads).
# On Render, mount a Disk at /var/data and set these as Environment Variables:
#
#   DATA_DIR=/var/data/data
#   UPLOADS_DIR=/var/data/uploads
#   CUSTOM_DIGITAL_DIR=/var/data/digital_custom_uploads
#
# You may alternatively set PERSIST_ROOT=/var/data and omit the individual dirs.

PERSIST_ROOT=
DATA_DIR=
UPLOADS_DIR=
CUSTOM_DIGITAL_DIR=
